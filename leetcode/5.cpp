#include <iostream>

using namespace std;

class Solution {
public:
    string longestPalindrome(string s) {
        ;
    }

private:
    string worker(string s, int kernell, int kernelr) {
        if (kernell == 0 || kernelr == s.length() - 1) return s;
    }
};

int main() {
    Solution sol;

    return 0;
}
